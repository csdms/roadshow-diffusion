[build-system]
requires = [
    "setuptools",
    "wheel",
    ]
build-backend = "setuptools.build_meta"

[project]
name = "roadshow-diffusion"
version = "0.5"
dependencies = [
    "matplotlib",
	"mpl_ascii",
    "numpy",
]

[tool.setuptools]
py-modules = ["roadshow_diffusion"]
